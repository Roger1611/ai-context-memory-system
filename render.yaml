services:
  - type: web
    name: ai-context-memory-api
    runtime: python
    pythonVersion: "3.11"
    buildCommand: pip install -r requirements-api.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port 10000
